cmake_minimum_required(VERSION 3.13)
project(task_3 C)
option(brack "compile brackets" OFF)

if(brack)
  message(STATUS "compile brack")
  add_executable(brack brack.c)
else()
  message(STATUS "compile counter zeros")
  add_executable(countz countz.c)
endif()
