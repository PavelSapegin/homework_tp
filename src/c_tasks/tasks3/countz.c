#include <stdio.h>

int main()
{
    int 10;
    int count = 0;
    int arr[10] = {0,1,2,3,4,0,0,1,1,2};
    for (int i = 0; i < n; ++i)
    {
        if (arr[i] == 0)
            count++;
    }

    printf("Количество нулей в массиве: %d шт.\n", count);

    return 0;
}






